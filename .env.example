# Copy to .env locally / set the same keys in Vercel Project Settings

# Render backend API (must include /api/v1)
NEXT_PUBLIC_API_URL=https://YOUR-RENDER-SERVICE.onrender.com/api/v1

# Same host origin (images / legacy paths)
NEXT_PUBLIC_IMAGE_URL=https://YOUR-RENDER-SERVICE.onrender.com
